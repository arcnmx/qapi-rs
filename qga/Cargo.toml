[package]
name = "qapi-qga"
version = "0.0.1"
build = "build.rs"

[build-dependencies]
qapi-codegen = { version = "^0.0.1", path = "../codegen" }

[dependencies]
serde = "^1.0.27"
serde_derive = "^1.0.27"
qapi-spec = { version = "^0.0.1", path = "../spec" }
